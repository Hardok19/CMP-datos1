// Nodo interno para la lista circular doblemente enlazada
public class Node {
    // Nodo interno para la lista circular doblemente enlazada
    Song data;
    Node next;
    Node prev;

    public Node(Song data) {
        this.data = data;
    }

}